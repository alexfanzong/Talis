; TuyaOpen project descriptor
; Maintained by the TuyaOpen IDE extension.
; Hand-editing is allowed — unknown keys are preserved on round-trips.

[project]
name = Talis
version = 0.1.0
created = 2026-08-15T08:02:36.644Z
type = tuyaopen-app
; INI format version — do not edit
schema = 1

[platform]
; valid targets: t5 | esp32 | linux | raspberrypi
target = t5ai
sdk = tuyaopen
sdk_version = main

[build]
output = build/
toolchain = default

[board]
id = sparkleiot-t5ai-dev
kconfig_id = SPARKLEIOT_T5AI_DEV
